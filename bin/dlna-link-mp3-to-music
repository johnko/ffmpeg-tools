#!/bin/sh

# READY

MP3PATH=/priv/data/beet/mp3/
MUSICPATH=/priv/data/music/

## can't link if on separate zfs dataset
# rsync -virtP --link-dest=${MP3PATH}  ${MP3PATH}  ${MUSICPATH}
rsync -virtP  ${MP3PATH}  ${MUSICPATH}
